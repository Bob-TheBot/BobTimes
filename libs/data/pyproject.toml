[project]
name = "data"
description = "Data utilities and models"
requires-python = ">=3.13"
version = "0.1.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["data"]